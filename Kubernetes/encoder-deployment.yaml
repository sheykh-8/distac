apiVersion: apps/v1
kind: Deployment
metadata:
  name: encoder
spec:
  replicas: 2
  selector:
    matchLabels:
      app: encoder
  template:
    metadata:
      labels:
        app: encoder
    spec:
      containers:
      - name: encoder
        image: sheykh8/tacotron_encoder:1.0.0
        ports:
        - containerPort: 80
